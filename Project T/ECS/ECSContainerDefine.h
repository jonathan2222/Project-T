#ifndef ECS_CONTAINER_DEFINE_H
#define ECS_CONTAINER_DEFINE_H

#ifndef Container
#define Container void* // The user sets this to something else, this needs to be set before including ECS.
#endif

#endif
